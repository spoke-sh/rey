name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  checks:
    name: Quality And Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: DeterminateSystems/determinate-nix-action@v3

      - uses: DeterminateSystems/flakehub-cache-action@main

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.cache/cargo-target/rey
          key: ${{ runner.os }}-${{ github.job }}-cargo-${{ hashFiles('Cargo.lock', 'flake.lock', 'flake.nix', '.github/workflows/ci.yml') }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-${{ github.job }}-cargo-${{ hashFiles('Cargo.lock', 'flake.lock', 'flake.nix', '.github/workflows/ci.yml') }}-
            ${{ runner.os }}-${{ github.job }}-cargo-

      - name: Run quality checks
        run: nix develop .#ci --command just check

      - name: Run tests
        run: nix develop .#ci --command just test

  nix-build:
    name: Nix Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: DeterminateSystems/determinate-nix-action@v3

      - uses: DeterminateSystems/flakehub-cache-action@main

      - name: Build Rey derivation
        run: nix build .#rey -L
